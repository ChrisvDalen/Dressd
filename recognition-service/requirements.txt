fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pillow==12.3.0
numpy==2.5.2
# Optional: heavy background-removal model. Uncomment to enable high-quality
# cut-outs; the service falls back to an offline colour-key when it is absent.
# rembg==2.0.59
# onnxruntime==1.20.1
