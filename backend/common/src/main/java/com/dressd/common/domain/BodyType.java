package com.dressd.common.domain;

/**
 * Fixed set of avatar silhouettes plus a CUSTOM option with adjustable
 * proportions (SPEC.md section 4, avatar domain model).
 */
public enum BodyType {
    SLIM,
    AVERAGE,
    CURVY,
    CUSTOM
}
