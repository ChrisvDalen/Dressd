package com.dressd.common.domain;

/**
 * The category a garment belongs to. Drives the per-category swipe rows in the
 * outfit builder (see SPEC.md flow B).
 */
public enum GarmentCategory {
    TOP,
    BOTTOM,
    SOCKS,
    LAYER,
    SHOES,
    ACCESSORY
}
