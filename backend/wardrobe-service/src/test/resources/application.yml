spring:
  datasource:
    url: jdbc:h2:mem:wardrobe;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

dressd:
  recognition:
    enabled: false
  storage:
    local:
      root: ./target/test-media
      public-base-url: /media
